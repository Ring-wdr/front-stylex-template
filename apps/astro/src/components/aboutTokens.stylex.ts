import * as stylex from '@stylexjs/stylex';

export const localAboutTokens = stylex.defineVars({
  demoInset: '20px',
  demoRadius: '24px',
  demoTint: 'rgba(251, 146, 60, 0.22)',
  demoStripe: 'rgba(254, 215, 170, 0.54)',
});
