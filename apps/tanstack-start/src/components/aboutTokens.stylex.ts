import * as stylex from '@stylexjs/stylex';

export const localAboutTokens = stylex.defineVars({
  demoInset: '20px',
  demoRadius: '24px',
  demoTint: 'rgba(45, 212, 191, 0.18)',
  demoStripe: 'rgba(153, 246, 228, 0.42)',
});
